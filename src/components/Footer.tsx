export default function Footer() {
  return (
    <footer className="flex items-center justify-between px-12 py-5 border-t border-[var(--rule)] bg-[var(--bg)]">
      <span className="font-serif italic text-sm text-[var(--muted)]">
        The Harold Collection
      </span>
      <span className="label opacity-50">Private Archive · Not for Commercial Use</span>
    </footer>
  )
}
